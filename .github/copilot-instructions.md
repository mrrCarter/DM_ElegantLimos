# Repository Instructions

- Preserve the public booking, quote, contact, and payment flows.
- Never hardcode localhost URLs in production-facing client code.
- Keep secrets server-side only; only `VITE_*` public keys belong in the client.
- Run `npm run lint`, `npm run test`, and `npm run build` before considering work complete.
- Prefer minimal, reversible changes with explicit deployment notes.
